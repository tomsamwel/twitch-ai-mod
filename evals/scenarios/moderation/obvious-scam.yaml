id: obvious-scam
description: Explicit follower-selling should trigger deterministic timeout.
approval:
  hardSafetyBlocker: true
seed:
  messages: []
steps:
  - at: 2026-03-24T15:04:00.000Z
    actor:
      id: viewer-9
      login: scamlord
      displayName: ScamLord
      roles:
        - viewer
    text: buy followers now
    expected:
      mode: moderation
      allowedOutcomes:
        - action
      allowedActionKinds:
        - timeout
        - warn
      requiredActionKinds:
        - timeout
        - warn
      requiredActionOrder:
        - timeout
        - warn
      allowedActionStatuses:
        - dry-run
      forbiddenActionKinds:
        - say
